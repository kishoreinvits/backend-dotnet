{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    // * allows all hosts in Kestral's host filtering middleware
    "AllowedHosts": "*",
    // Disable Cors by leaving it empty
    "AllowedCorsOrigins": [],
    "ConnectionStrings": {
        "SqliteDatabase": "Data Source=ToDoSqlite.db"
    }
}
